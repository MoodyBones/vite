{
  "name": "tdd-practice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose",
    "prepare": "cd .. && husky install"
  },
  "devDependencies": {
    "@types/jest": "29.4.1",
    "eslint": "^8.36.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "prettier": "^2.8.4",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "jest": "^29.5.0"
  },
  "lint-staged": {
    "*.{js,ts,css,md,vue}": "prettier --write"
  }
}
